# 実験結果の検証報告

## 要約

| 項目 | 検証結果 | 備考 |
|------|----------|------|
| **表1（関節角度MAE）** | 現行データに更新済み | 出典: 03_joint_angle_mae coordinate_angle_mae（データ出典記録.md 参照） |
| **表2・表3（相関）** | 一致 | 05_direction_detection 出力と整合 |
| **方向角誤差（Δθ, Δψ）** | 一致 | 肘58°、肩10°、腰ψ90°等、論文の記述と整合 |

---

## 1. 表1（関節角度MAE）

**正本**: `paper/source/IEEJ_ja/main.tex` の表（例：左肩 MAE 39.8°）。旧稿（35.4° 台）との差はデータ版・集計手順の違いによる。

`03_joint_angle_mae` の 2 層 `coordinate_angle_mae.csv` を結合した全カメラ平均は、上記 IEEJ_ja 表と一致する想定。`python verify_paper_data.py` で突合せる。

---

## 2. 方向角誤差（表2・3、考察）の検証

`05_direction_detection` の `joint_summary.csv` および相関分析結果と照合しました。

| 指標 | 論文 | 05_direction_detection | 結果 |
|------|------|------------------------|------|
| 肘 \|Δθ\| 平均 | 約58° | 左58.3°、右59.3° | ✓ 一致 |
| 肩 \|Δθ\| 平均 | 約10--12° | 左10.4°、右9.3° | ✓ 一致 |
| 腰 \|Δψ\| 平均 | 約90° | 左88.9°、右91.1°（別計算） | ✓ 一致 |
| LEFT_HIP–RIGHT_HIP 相関 | r = -0.84 | -0.8402 | ✓ 一致 |
| LEFT_ELBOW–RIGHT_ELBOW 相関 | r = -0.862 | -0.8622 | ✓ 一致 |

方向角誤差と相関に関する記述は、現行データと整合しています。

---

## 3. データソース整理

| 論文の要素 | データソース |
|------------|--------------|
| 表1（関節角度MAE） | `03_joint_angle_mae/.../coordinate_angle_mae.csv`（2層結合） |
| 表2・表3、ヒートマップ | `05_direction_detection`（Y軸反転適用済み） |
| 考察の数値 | `05_direction_detection` |

---

## 4. 再現手順

```bash
# 方向角誤差・相関の検証
cd Zeval_DataSet
python 05_direction_detection/process_all_data.py
python 05_direction_detection/scripts/compute_correlation.py

# 表1の検証（要：03_joint_angle_mae 用 MAE CSV の生成）
python verify_paper_data.py
```
