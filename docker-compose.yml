version: '3.8'

services:
  dashboard:
    build:
      context: .
      dockerfile: docker/Dockerfile
    ports:
      - "8050:8050"
    environment:
      - DASH_HOST=0.0.0.0
      - DASH_DEBUG=false
    # データをボリュームで渡す場合（ローカルで生成済みの output を使用）
    # volumes:
    #   - ./5_direction/output:/app/5_direction/output:ro
    #   - ./synced_joint_positions.csv:/app/synced_joint_positions.csv:ro
